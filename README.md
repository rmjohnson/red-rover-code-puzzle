# Red Rover Code Puzzle

Make sure to run `npm i` before running the program or tests.

Run the CLI program with `npm run start <input> [sorted]`. `[sorted]` is an optional parameter. Make sure your `<input>` is in quotes so it will be a single argument to the CLI.

Use `npm run test` to run the tests.
